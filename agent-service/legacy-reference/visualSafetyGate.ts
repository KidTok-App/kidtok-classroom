/**
 * COMPLIANCE STUB — original reference module removed from the working tree.
 *
 * The original `visualSafetyGate.ts` referenced external vendors that are not part of
 * the shipped Google-only runtime, so its content was stripped per the
 * hackathon runtime mandate after its reusable logic was ported to:
 *
 *   src/clients/imageSafety.ts (gate semantics: fail-open on infra errors, throw on positive unsafe; MIME sniffing)
 *
 * See agent-service/legacy-reference/PORTING.md for the full porting map.
 * The original file is preserved in git history.
 */
export {};
